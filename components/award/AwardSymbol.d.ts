import { ReactElement } from '../../../node_modules/react';
interface ArrowDownSymbolProps {
    size?: number;
    color?: string;
}
export declare function AwardSymbol({ size, color, }: ArrowDownSymbolProps): ReactElement;
export {};
