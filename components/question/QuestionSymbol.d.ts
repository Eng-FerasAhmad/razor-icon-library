import { ReactElement } from '../../../node_modules/react';
interface MessageIconProps {
    size?: number;
    color?: string;
}
export declare function QuestionSymbol({ size, color, }: MessageIconProps): ReactElement;
export {};
