import { ReactElement } from '../../../../node_modules/react';
interface SearchIconProps {
    size?: number;
    color?: string;
}
export declare function SearchOutline({ size, color, }: SearchIconProps): ReactElement;
export {};
