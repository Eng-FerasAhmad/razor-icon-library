import { ReactElement } from '../../../../node_modules/react';
interface EditIconProps {
    size?: number;
    color?: string;
}
export declare function EditOutline({ size, color, }: EditIconProps): ReactElement;
export {};
