import { ReactElement } from '../../../node_modules/react';
interface Props {
    size?: number;
    color?: string;
}
export declare function MailTwotone({ size, color, }: Props): ReactElement;
export {};
