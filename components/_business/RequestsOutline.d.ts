import { ReactElement } from '../../../node_modules/react';
interface CustomIconProps {
    size?: number;
    color?: string;
}
export declare function RequestsOutline({ size, color, }: CustomIconProps): ReactElement;
export {};
