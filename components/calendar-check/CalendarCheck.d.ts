import { ReactElement } from '../../../node_modules/react';
interface CustomSVGIconProps {
    size?: number;
    color?: string;
}
export declare function CalendarCheckSymbol({ size, color, }: CustomSVGIconProps): ReactElement;
export {};
