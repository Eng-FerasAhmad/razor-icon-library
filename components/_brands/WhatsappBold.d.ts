import { ReactElement } from '../../../node_modules/react';
interface Props {
    size?: number;
    color?: string;
}
export declare function WhatsappBold({ size, color, }: Props): ReactElement;
export {};
