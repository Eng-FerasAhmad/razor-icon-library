import { ReactElement } from '../../../node_modules/react';
interface AlarmSymbolProps {
    size?: number;
    color?: string;
}
export declare function TimerSymbol({ size, color, }: AlarmSymbolProps): ReactElement;
export {};
