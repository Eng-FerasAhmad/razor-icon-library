import { ReactElement } from '../../../node_modules/react';
interface EditIconProps {
    size?: number;
    color?: string;
}
export declare function EditUserSymbol({ size, color, }: EditIconProps): ReactElement;
export {};
