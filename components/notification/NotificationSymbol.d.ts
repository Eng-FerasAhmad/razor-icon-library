import { ReactElement } from '../../../node_modules/react';
interface NotificationIconProps {
    size?: number;
    color?: string;
}
export declare function NotificationSymbol({ size, color, }: NotificationIconProps): ReactElement;
export {};
