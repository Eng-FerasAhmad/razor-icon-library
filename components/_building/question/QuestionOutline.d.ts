import { ReactElement } from '../../../../node_modules/react';
interface MessageIconProps {
    size?: number;
    color?: string;
}
export declare function QuestionOutline({ size, color, }: MessageIconProps): ReactElement;
export {};
