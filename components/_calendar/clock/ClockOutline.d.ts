import { ReactElement } from '../../../../node_modules/react';
interface ClockSymbolProps {
    size?: number;
    color?: string;
}
export declare function ClockOutline({ size, color, }: ClockSymbolProps): ReactElement;
export {};
