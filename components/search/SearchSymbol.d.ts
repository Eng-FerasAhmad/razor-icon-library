import { ReactElement } from '../../../node_modules/react';
interface SearchIconProps {
    size?: number;
    color?: string;
}
export declare function SearchSymbol({ size, color, }: SearchIconProps): ReactElement;
export {};
