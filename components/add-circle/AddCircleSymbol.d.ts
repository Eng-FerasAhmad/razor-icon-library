import { ReactElement } from '../../../node_modules/react';
interface IconProps {
    size?: number;
    color?: string;
}
export declare function AddCircleSymbol({ size, color, }: IconProps): ReactElement;
export {};
