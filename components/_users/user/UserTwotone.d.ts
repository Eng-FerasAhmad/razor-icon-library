import { ReactElement } from '../../../../node_modules/react';
interface Props {
    size?: number;
    color?: string;
}
export declare function UserTwotone({ size, color, }: Props): ReactElement;
export {};
