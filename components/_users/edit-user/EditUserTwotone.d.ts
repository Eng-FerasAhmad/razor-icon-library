import { ReactElement } from '../../../../node_modules/react';
interface EditIconProps {
    size?: number;
    color?: string;
}
export declare function EditUserTwotone({ size, color, }: EditIconProps): ReactElement;
export {};
