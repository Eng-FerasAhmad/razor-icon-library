import { ReactElement } from '../../../node_modules/react';
interface CloseIconProps {
    size?: number;
    color?: string;
}
export declare function CloseSymbol({ size, color, }: CloseIconProps): ReactElement;
export {};
