import { ReactElement } from '../../../node_modules/react';
interface EditIconProps {
    size?: number;
    color?: string;
}
export declare function EditSymbol({ size, color, }: EditIconProps): ReactElement;
export {};
